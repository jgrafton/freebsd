# TODO for documenting ZFS encryption enabling on adduser

## adduser changes
- (draft) note that a change is required for ZFS encryption in adduser.8 man page
- add commented out lines to /etc/pam.d/system
  - test logging in
- add warning within adduser itself

## can i figure out what's going on with the double password?
- is the double password an issue that can be fixed with the current code base?

